anthropic>=0.18.0
openai>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pillow>=10.0.0


# Compositor (scene insertion / occlusion pipeline)
numpy>=1.24.0
opencv-python>=4.8.0
 
# Compositor — scene preprocessing only (heavy ML deps, lazy-imported)
torch>=2.0.0
transformers>=4.40.0
sam2 @ git+https://github.com/facebookresearch/sam2.git